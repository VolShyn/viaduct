import { TOAST_HEIGHT_PX } from '@components/ToastNotification';

/** Gap between the toast and the error bar stacked under it. */
export const STACK_GAP_PX = 8;

/** Re-exported so ErrorBox stays aligned if the toast height changes. */
export { TOAST_HEIGHT_PX };
