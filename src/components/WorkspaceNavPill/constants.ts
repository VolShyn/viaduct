export const NAV_ICON_SIZE = 14;

/** A crumb gives up width before the pill does. */
export const CRUMB_LABEL_MAX_W = { base: '100px', sm: '160px', md: '240px' };
export const NAME_FIELD_MAX_W = '200px';

/** Every control in the pill, and the pill itself. */
export const PILL_MIN_H = '40px';
export const PILL_CONTROL_H = '28px';

export const DIVIDER_HEIGHT = '18px';
export const DIVIDER_OPACITY = 0.85;

/** A project always has a name, even when the field is emptied. */
export const DEFAULT_PROJECT_NAME = 'Untitled project';
