/** Both the menu and its submenus, so a submenu never looks narrower. */
export const NODE_MENU_MIN_WIDTH = '220px';

/** A disabled row stays readable rather than disappearing. */
export const DISABLED_ITEM_OPACITY = 0.45;

export const SUBMENU_CHEVRON_SIZE = 18;
export const SUBMENU_GUTTER = 2;
