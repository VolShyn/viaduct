/** Menu box: wide enough for a two-line row, short enough to stay on screen. */
export const ADD_MENU_MIN_WIDTH = '280px';
export const ADD_MENU_MAX_HEIGHT = 'min(70vh, 480px)';

/** Placeholder names for the "empty" rows — the user renames them right after. */
export const DEFAULT_TABLE_NAME = 'New Table';
export const DEFAULT_PERSON_NAME = 'User';
export const DEFAULT_ENDPOINT_NAME = 'New Endpoint';
export const DEFAULT_ENDPOINT_METHOD = 'GET';
export const DEFAULT_ENDPOINT_PATH = '/api/resource';
export const DEFAULT_CHANNEL_SCHEMA_FORMAT = 'avro';
