export const INSTRUMENTS_ITEM_ICON_SIZE = 15;
export const INSTRUMENTS_TRIGGER_ICON_SIZE = 14;
export const INSTRUMENTS_CHEVRON_SIZE = 12;

/** Dropdown box: width, and the gap below the trigger it hangs from. */
export const INSTRUMENTS_MENU_MIN_WIDTH = '210px';
export const INSTRUMENTS_MENU_DROP_GAP_PX = 6;

/** An item already open stays visible, greyed, rather than disappearing. */
export const INSTRUMENTS_DIMMED_OPACITY = 0.45;

/*
 * Where to come back to once the overlay is left. Read by DataFlowsPage and
 * ServiceCatalogPage, which own the return journey.
 */
export const FLOWS_RETURN_KEY = 'c4-flows-return';
export const CATALOG_RETURN_KEY = 'c4-catalog-return';
