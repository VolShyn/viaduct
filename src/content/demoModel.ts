/**
 * A share link to a real, populated model that anyone can open without an
 * account — the strongest evidence this page can carry, and the one piece of
 * it that cannot be written here.
 *
 * To fill it in: open the project you want strangers to see, create a share
 * link with view-only access, and paste it below. Anyone holding the link sees
 * that project, so point it at a model built for the purpose rather than at a
 * client's architecture, and remember that revoking the link from the share
 * panel takes the section off the page.
 *
 * Empty means the section does not render at all. A dead or wrong link is
 * worse than no link.
 */
export const DEMO_SHARE_URL = '';
