import EditorWorkspace, { type EditorWorkspaceProps } from '@widgets/editor-workspace/EditorWorkspace';

export default function EditorPage(props: EditorWorkspaceProps) {
  return <EditorWorkspace {...props} />;
}
